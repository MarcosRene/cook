import { StyleSheet } from 'react-native'

import { theme } from '@/theme'

export const styles = StyleSheet.create({
  container: {
    paddingBottom: 200,
    flexWrap: 'wrap',
    flexDirection: 'row',
    gap: 12,
  },
})
